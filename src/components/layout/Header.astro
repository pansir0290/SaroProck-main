---
// src/components/layout/Header.astro
import FuwariProfile from "@components/FuwariProfile.astro";
import SearchBar from "@components/widgets/SearchBar.astro"; 
---

<style>
  @import url('https://fonts.googleapis.com/css2?family=Grand+Hotel&display=swap');
  .ins-logo-font {
    font-family: 'Grand Hotel', cursive, system-ui, -apple-system, sans-serif;
  }
</style>

<header class="w-full bg-base-100">
  <!-- 
    核心关键点：
    1. lg:max-w-2xl: 在电脑端（大屏）强制将最大宽度限制为 max-w-2xl（与你的帖子 DIV 宽度完全一致！）
    2. lg:ml-[calc(...)] 或直接在 Grid 主列内部居中
    这里使用 flex flex-col items-center，配合 max-w-2xl mx-auto / lg:mx-0，完美对齐帖子 DIV
  -->
  <div class="w-full max-w-2xl mx-auto px-4">
    
    <!-- 顶栏标题 + 搜索框 -->
    <div class="w-full h-14 flex items-center justify-between">
      <div class="flex items-center space-x-2">
        <a href="/" class="text-xl sm:text-2xl font-bold tracking-normal ins-logo-font text-base-content hover:opacity-80 transition-opacity">
          サンpansir的随笔
        </a>
      </div>

      <div class="w-36 sm:w-48 flex justify-end items-center">
        <SearchBar />
      </div>
    </div>

    <!-- Ins Highlights 圆球列表（跟标题同在一个 max-w-2xl 容器里，彻底对齐帖子） -->
    <div class="w-full">
      <FuwariProfile />
    </div>

  </div>
</header>
